import React from "react";
import { ReleaseNoteType } from "../types";
interface Props {
    handleChange: (arg0: ReleaseNoteType) => void;
}
declare const ProjectView: React.FC<Props>;
export default ProjectView;
